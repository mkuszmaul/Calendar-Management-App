package calendar.controller;

/**
 * An interface for the controller for the calendar app. Has just one method
 * to begin the app.
 */
public interface IController {
  /**
   * Start the CalendarApp.
   */
  void begin();
}
